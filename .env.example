# ==================== Frontend Environment Template ====================
# Template for environment variables used by Next.js frontend
# Copy this file to .env.local for local development
#
# SETUP: cp .env.example .env.local
#
# IMPORTANT:
# - NEVER commit .env.local, .env.development, or any .env files with actual values
# - ONLY this .env.example template should be committed to git
# - All variables with NEXT_PUBLIC_ prefix are exposed to browser JavaScript
# ========================================================================

# ==================== API Configuration ====================
# Backend API URL
# NEXT_PUBLIC_ prefix makes this accessible in browser code
# Development: Point to local backend (usually http://localhost:3005/api/v1)
# Production: Set via Docker build args or deployment platform
NEXT_PUBLIC_API_URL=http://localhost:3005/api/v1
